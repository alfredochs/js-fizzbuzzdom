const containerTabella = document.getElementById("container-tabella");

for (let i = 1; i <= 7; i++) {
    const row = document.createElement("div");
    row.classList.add("row", "g-0", "bg-danger");

    for (let x = 1; x <= 100; x++) {

    }
}